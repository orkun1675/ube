{
  "name": "ube-landing",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Static landing for ube.dev. Local: open index.html. CI: npm run build.",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}