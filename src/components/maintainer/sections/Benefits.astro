---
// Benefits — "Outcomes" section with stat callout and 6 content cards.
// Icons render as inline SVG via the React .tsx icon components (no client:*
// directive → zero JS shipped).
//
// Layout-specific styling lives in the scoped <style> block at the bottom
// of this file (Astro auto-scopes per-component, so class names won't leak).
import {
  ChatCircleDotsIcon,
  RocketLaunchIcon,
  ShieldCheckIcon,
  SparkleIcon,
  StarIcon,
  TrendUpIcon,
} from "@phosphor-icons/react"
import Term from "@/components/Term.astro"
import type { GlossaryKey } from "@/data/glossary"

// Card bodies are fragment arrays so jargon renders as an inline tap <Term>.
type BodyPart = string | { term: GlossaryKey; label?: string }
type Card = {
  Icon: typeof SparkleIcon
  title: string
  body: BodyPart[]
}

const cards: Card[] = [
  {
    Icon: SparkleIcon,
    title: "Reclaim engineering time.",
    body: [
      "Stop spending a third of every sprint debugging race conditions you didn't write. Hand the boring triage to Ube and continue shipping delightful features.",
    ],
  },
  {
    Icon: ChatCircleDotsIcon,
    title: "Your backlog stays lean.",
    body: [
      "Every incoming bug report is triaged, fixed, or followed up on, automatically. Nothing rots in the queue, so your task list stays short.",
    ],
  },
  {
    Icon: ShieldCheckIcon,
    title: "Catch bugs early.",
    body: [
      "Ube becomes your manual ",
      { term: "qa" },
      " agent, clicks around the app like a human would. You no longer need to wait for a beta or alpha release to discover bugs.",
    ],
  },
  {
    Icon: TrendUpIcon,
    title: "Stay under Play's bad-behavior threshold.",
    body: [
      "Apps crashing on more than 1.09% of sessions or hitting ",
      { term: "anr", label: "ANRs" },
      " above 0.47% get demoted in Play discoverability.",
    ],
  },
  {
    Icon: StarIcon,
    title: "Higher app-store ratings.",
    body: [
      "Faster fixes plus a personal follow-up nudges users back into the review flow. Updated reviews compound into a better average rating for your app.",
    ],
  },
  {
    Icon: RocketLaunchIcon,
    title: "Keep tech debt in check.",
    body: [
      "Agentic coding ships features faster than ever, and quietly piles up bugs and shortcuts. Ube does the unglamorous cleanup in the background so velocity doesn't come to a halt.",
    ],
  },
]
---

<section class="section benefits-section">
  <div class="container">
    <div class="eyebrow benefits-eyebrow">
      Outcomes
    </div>
    <div class="benefits-head">
      <h2 class="t-display-lg benefits-headline">
        Less time maintaining.
        <br />
        More time building.
      </h2>
      <div class="benefits-stat-row">
        <div class="t-display-mega benefits-stat">
          10-30%
        </div>
        <div class="t-body-md body benefits-stat-caption">
          of your team's engineering time reclaimed, based on early teams
          running Ube.
        </div>
      </div>
    </div>

    <div class="grid-3 benefits-grid">
      {
        cards.map(({ Icon, title, body }) => (
          <div class="card card-hover benefits-card">
            <div class="benefits-icon">
              <Icon size={20} aria-hidden="true" />
            </div>
            <h3 class="t-title-md ink benefits-card-title">
              {title}
            </h3>
            <p class="t-body-sm body benefits-card-body">
              {
                body.map((part) =>
                  typeof part === "string" ? (
                    part
                  ) : (
                    <Term term={part.term} label={part.label} />
                  ),
                )
              }
            </p>
          </div>
        ))
      }
    </div>
  </div>
</section>

<style>
  .benefits-section {
    background: var(--canvas);
  }
  .benefits-eyebrow {
    margin-bottom: 18px;
  }
  .benefits-head {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 60px;
    align-items: end;
  }
  .benefits-headline {
    margin: 0;
    max-width: 520px;
  }
  .benefits-stat-row {
    display: flex;
    align-items: flex-end;
    gap: 20px;
  }
  .benefits-stat {
    color: var(--accent);
    font-size: clamp(40px, 7vw, 70.4px);
    line-height: 1;
    letter-spacing: -3px;
    white-space: nowrap;
  }
  .benefits-stat-caption {
    max-width: 230px;
    text-wrap: balance;
  }
  .benefits-grid {
    margin-top: 64px;
  }
  .benefits-card {
    padding: 24px;
  }
  .benefits-icon {
    width: 40px;
    height: 40px;
    border-radius: 8px;
    background: var(--mark-tint);
    color: var(--mark);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 20px;
    border: 1px solid color-mix(in oklab, var(--mark) 25%, transparent);
  }
  .benefits-card-title {
    margin-bottom: 10px;
  }
  .benefits-card-body {
    margin: 0;
    line-height: 1.6;
  }
  @media (max-width: 1024px) {
    .benefits-head {
      grid-template-columns: 1fr;
      gap: 32px;
      align-items: start;
    }
    .benefits-head > *:last-child {
      justify-self: end;
      justify-content: flex-end;
      text-align: right;
    }
    .benefits-stat {
      letter-spacing: -2px;
    }
  }
  @media (max-width: 640px) {
    .benefits-stat {
      font-size: clamp(27px, 10.2vw, 41px);
      letter-spacing: -1.5px;
      max-width: 50vw;
    }
    .benefits-stat-caption {
      font-size: clamp(11px, 3.1vw, 14px);
      line-height: 1.35;
      max-width: 44vw;
      display: -webkit-box;
      -webkit-line-clamp: 3;
      -webkit-box-orient: vertical;
      overflow: hidden;
      text-wrap: balance;
    }
  }
</style>
